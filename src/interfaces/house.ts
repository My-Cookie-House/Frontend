export interface IHouseOutside {
  icingId: number;
  cookieIds: number[];
  houseName: string;
}

export interface IMutateHouse {
  icingId: number;
  cookieIds: number[];
  houseName: string;
  wallpaperId: number;
}
